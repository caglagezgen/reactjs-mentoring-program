import { createUseStyles } from 'react-jss';

const style = createUseStyles({
  appContainer: {
    height: '100%',
    margin: 'auto',
    position: 'relative',
    width: '1200px',
  },
});

export default style;
